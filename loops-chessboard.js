// sketch.js
function setup() {
  createCanvas(500,400)
}

function draw() {
  var x = 50
  var count = 0

  background(0)
  fill(250)
  
for (var count; count < 8; count++) {
  rect(x, height/2, 40, 40)
  x += 40; 
    }
    if (square > 1) {
        return true;
    }
}
